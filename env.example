# Copy to `.env` for local development:
#   cp env.example .env
#
# `docker compose` reads `.env` automatically from the repo root.

# App / ports
DJANGO_PORT=8000
VUE_PORT=5173

# Postgres
POSTGRES_DB=jarcode
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
POSTGRES_HOST=db

# Redis
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_DEFAULT_USER=user
RABBITMQ_DEFAULT_PASS=user
RABBITMQ_AMQP_PORT=5672
RABBITMQ_UI_PORT=15672

# Email (optional; used for activation/password reset)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# AI (optional)
GEMINI_API_KEY=

